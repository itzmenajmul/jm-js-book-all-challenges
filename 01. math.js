// Earning and expence
const earning = 100;
const expence = 40;
const remainingMoney = (earning - expence);

// use store pencils
const IHavePencil = 10;
const everyTimeIuse = 2;
const howManyTimeICanUsePencil = (IHavePencil / everyTimeIuse);

// saving and expence
const IHaveMoney = 60;
const everyTimeCost = 15;
const HowManyTimeIExpendMoney = (IHaveMoney / everyTimeCost);
const HowMuchMoneyRemain = (IHaveMoney % everyTimeCost);

// Hello World
const hello = "Hello";
const world = "World";
const helloworld = (hello + world);

// Chal and Dal er hisab
const rice = 153;
const lantis = 261;
const totalKg = (rice + lantis);

// again saving and expences er hisab
const totalSaving = 500;
const everyTimeFare = 75;
const HowManyTimeICanDoFare = (totalSaving / everyTimeFare);
const HowMuchLeftFromSaving = (totalSaving % everyTimeFare);

// 8 or 3 division and remaining
const eight = 8;
const three = 3;
const division = (eight / three);
const eightThreeRemaining = (eight % three);

// 50 and 9 er remaining
const fifty = 50;
const nine = 9;
const fiftyNineRemaining = (fifty % nine);

// Bangla and desh
const bangla = "Bangla";
const desh = "desh";
const fullBangladesh = (bangla + desh);

// Rice store and predictable days
const perDayEatRice = 4;
const totalRice = 480;
const everyMonthDays = 30;
const predictableDays = (totalRice / perDayEatRice);
const totalMonths = ((perDayEatRice * everyMonthDays) / totalRice)

console.log(remainingMoney);
console.log(howManyTimeICanUsePencil);
console.log(HowManyTimeIExpendMoney);
console.log(HowMuchMoneyRemain);
console.log(helloworld);
console.log(totalKg);
console.log(HowManyTimeICanDoFare);
console.log(HowMuchLeftFromSaving);
console.log(division);
console.log(eightThreeRemaining);
console.log(fiftyNineRemaining);
console.log(fullBangladesh);
console.log(predictableDays);
console.log(totalMonths)